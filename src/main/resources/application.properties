# Application
spring.application.name=eb-demo-app

# Server – Elastic Beanstalk expects the app on port 5000 for Java SE platform
server.port=${PORT:5000}

# AWS – values injected by Elastic Beanstalk environment variables
aws.region=${AWS_REGION:us-east-1}
s3.bucket.name=${S3_BUCKET_NAME:}
